---@meta
-- AUTO-GENERATED by LuaFactorySchemaRegistry. Do not edit by hand.
-- Source of truth: hob:: bind_factory_schema(...) calls in lua_bind_*.cpp.

--- Assigning `DefineAnimationClip.Foo = { ... }` registers a
--- named AnimationClip configuration. Reference it as `AnimationClips.Foo` in prefabs and config; the actual AnimationClip
--- is built lazily on first unwrap, so DefineAnimationClip calls can live in any file in any load order.
--- When passing `AnimationClips.Foo` directly to a C++ setter, unwrap with `unwrap_def(...)`.
---@class DefineAnimationClip
DefineAnimationClip = {}

--- Registry of AnimationClip configs declared via `DefineAnimationClip`. `AnimationClips.Foo` returns a deferred reference
--- that resolves to the built AnimationClip via unwrap_def (see engine/scripts/unwrap.lua).
---@class AnimationClips
AnimationClips = {}

--- Assigning `DefineTexture.Foo = { ... }` registers a
--- named Texture configuration. Reference it as `Textures.Foo` in prefabs and config; the actual Texture
--- is built lazily on first unwrap, so DefineTexture calls can live in any file in any load order.
--- When passing `Textures.Foo` directly to a C++ setter, unwrap with `unwrap_def(...)`.
---@class DefineTexture
DefineTexture = {}

--- Registry of Texture configs declared via `DefineTexture`. `Textures.Foo` returns a deferred reference
--- that resolves to the built Texture via unwrap_def (see engine/scripts/unwrap.lua).
---@class Textures
Textures = {}

--- Assigning `DefineShader.Foo = { ... }` registers a
--- named Shader configuration. Reference it as `Shaders.Foo` in prefabs and config; the actual Shader
--- is built lazily on first unwrap, so DefineShader calls can live in any file in any load order.
--- When passing `Shaders.Foo` directly to a C++ setter, unwrap with `unwrap_def(...)`.
---@class DefineShader
DefineShader = {}

--- Registry of Shader configs declared via `DefineShader`. `Shaders.Foo` returns a deferred reference
--- that resolves to the built Shader via unwrap_def (see engine/scripts/unwrap.lua).
---@class Shaders
Shaders = {}

--- Assigning `DefineMaterial.Foo = { ... }` registers a
--- named Material configuration. Reference it as `Materials.Foo` in prefabs and config; the actual Material
--- is built lazily on first unwrap, so DefineMaterial calls can live in any file in any load order.
--- When passing `Materials.Foo` directly to a C++ setter, unwrap with `unwrap_def(...)`.
---@class DefineMaterial
DefineMaterial = {}

--- Registry of Material configs declared via `DefineMaterial`. `Materials.Foo` returns a deferred reference
--- that resolves to the built Material via unwrap_def (see engine/scripts/unwrap.lua).
---@class Materials
Materials = {}

